# Secretos Google - NUNCA subir al repo
api/credentials.json
api/token.pickle

# Python
__pycache__/
*.pyc
*.pyo
.env

# OS
.DS_Store
Thumbs.db

# Paquete de idioma OCR (18MB, se descarga aparte, no va al repo)
tessdata/

# Bajadas y salidas del modulo de descuento de certificados (13MB). Mismo
# criterio que tessdata/: el PDF de CAMARCO se vuelve a bajar solo (lo pide la
# API de medios) y el CSV lo regenera cargar_camarco_certificados.py, asi que
# versionarlos es guardar 13MB de algo reproducible. Ademas los dos PDF que
# habia eran EL MISMO archivo con dos nombres (MD5 identico, 6,3MB cada uno).
datos/

# Estado de runtime — vive en C:\SIBRA\<modulo>, no en el repo
# (ver SIBRA_SERVER/PROCESO.md). No sincronizar por OneDrive ni versionar.
token.pickle
*.log
perfil_chrome/
*.sqlite3

# Lo que se baja de la fuente: el PDF del indice de CAMARCO, el de tasas del
# BNA y su marca de ultima carga. Son 13 MB que el propio codigo vuelve a
# bajar (`cargar_camarco_certificados.py` los pide a la API de medios del
# sitio), y la convencion de la casa es que las bases van a
# H:\My Drive\web_sibra\, no al repo.
datos/
